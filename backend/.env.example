# Server Settings
PORT=5000
FRONTEND_URL=http://localhost:5173
JWT_SECRET=mailmate_super_secret_jwt_key_99887766

# Database configuration
# Leave empty to automatically run in In-Memory Mock Database Mode (no setup needed)
MONGODB_URI=

# Google OAuth 2.0 Credentials
# Required for connecting to real Gmail inbox data
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5000/api/auth/google/callback

# Google Gemini API Key
# Optional: If provided, generates live summaries. Otherwise uses local smart templates.
GEMINI_API_KEY=
